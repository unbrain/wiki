---
title: unbrain
description: Senior Front-end Developer · JavaScript · Vue.js
aliases: []
tags:
  - index
---

<div class="noise"></div>
<div class="cursor" id="ub-cursor"></div>
<div class="cursor-follower" id="ub-cursorFollower"></div>
<div class="progress-bar" id="ub-progressBar"></div>

<nav class="ub-nav" id="ub-nav">
  <div class="ub-nav-logo">UNBRAIN</div>
  <div class="ub-nav-links">
    <a href="#ub-about">Profile</a>
    <a href="#ub-knowledge">Knowledge</a>
    <a href="#ub-connect">Connect</a>
  </div>
</nav>

<!-- HERO -->
<section class="ub-hero" id="ub-hero">
  <canvas id="ub-particleCanvas"></canvas>
  <div class="ub-hero-slash ub-hero-slash-1"></div>
  <div class="ub-hero-slash ub-hero-slash-2"></div>
  <div class="ub-hero-content">
    <div class="ub-hero-tag">
      <span class="ub-tag-bracket">[</span> Front-end Developer · Algorithm · Vue.js <span class="ub-tag-bracket">]</span>
    </div>
    <h1 class="ub-hero-name">
      <span class="ub-line-wrap"><span class="ub-reveal-line">unbrain.</span></span>
    </h1>
    <p class="ub-hero-sub reveal-fade">Write code. Study algorithms. Ship things.</p>
    <div class="ub-hero-socials reveal-fade">
      <a href="https://github.com/unbrain" target="_blank">GH</a>
      <a href="mailto:marsorsun@gmail.com">ML</a>
    </div>
    <div class="ub-hero-scroll-hint">
      <span>▼ SCROLL ▼</span>
    </div>
  </div>
  <div class="ub-hud-corner ub-hud-tl"></div>
  <div class="ub-hud-corner ub-hud-tr"></div>
  <div class="ub-hud-corner ub-hud-bl"></div>
  <div class="ub-hud-corner ub-hud-br"></div>
</section>

<!-- MARQUEE -->
<div class="ub-marquee-strip">
  <div class="ub-marquee-inner">
    VUE.JS · TYPESCRIPT · ALGORITHMS · NODE.JS · LEETCODE · DATA STRUCTURES · FRONT-END · ENGINEERING · VUE.JS · TYPESCRIPT · ALGORITHMS · NODE.JS · LEETCODE · DATA STRUCTURES · FRONT-END · ENGINEERING ·&nbsp;
  </div>
</div>

<!-- ABOUT -->
<section class="ub-about" id="ub-about">
  <div class="ub-container">
    <div class="ub-section-label ub-scroll-reveal"><span class="ub-label-num">01</span> CHARACTER PROFILE</div>
    <div class="ub-about-grid">
      <div class="ub-about-left ub-scroll-reveal">
        <h2 class="ub-section-title">Senior<br/>Front-end<br/>Developer.</h2>
        <div class="ub-char-stats">
          <div class="ub-char-stat-row">
            <span class="ub-char-stat-name">VUE — Framework Mastery</span>
            <div class="ub-stat-bar"><div class="ub-stat-bar-fill" data-width="95"></div></div>
          </div>
          <div class="ub-char-stat-row">
            <span class="ub-char-stat-name">ALG — Algorithm & DSA</span>
            <div class="ub-stat-bar"><div class="ub-stat-bar-fill" data-width="82"></div></div>
          </div>
          <div class="ub-char-stat-row">
            <span class="ub-char-stat-name">TS — TypeScript</span>
            <div class="ub-stat-bar"><div class="ub-stat-bar-fill" data-width="88"></div></div>
          </div>
          <div class="ub-char-stat-row">
            <span class="ub-char-stat-name">ENG — Engineering</span>
            <div class="ub-stat-bar"><div class="ub-stat-bar-fill" data-width="85"></div></div>
          </div>
        </div>
      </div>
      <div class="ub-about-right">
        <p class="ub-about-text ub-scroll-reveal">
          Hi. I'm a Senior Front-end Developer with years building large-scale web products.
          Previously led frontend at Weibo — PC main site, creator center, DM inbox.
        </p>
        <p class="ub-about-text ub-scroll-reveal">
          Outside of work, I spend time studying algorithms and data structures.
          This site is my public learning log — 70+ notes covering LeetCode Hot 100,
          Classic 150, DSA fundamentals, and engineering practice.
        </p>
        <p class="ub-about-text ub-scroll-reveal">
          Stack: Vue 3 · TypeScript · Vite · Node.js · Egg.js · Redis.
        </p>
      </div>
    </div>
  </div>
</section>

<!-- KNOWLEDGE -->
<section class="ub-knowledge" id="ub-knowledge">
  <div class="ub-container">
    <div class="ub-section-label ub-scroll-reveal"><span class="ub-label-num">02</span> SKILL SCROLLS</div>
    <h2 class="ub-section-title ub-scroll-reveal">My Notes.</h2>
    <div class="ub-writing-list">
      <a class="ub-writing-item ub-scroll-reveal" href="/JavaScript 算法/算法基础/JavaScript 算法基础第一天">
        <span class="ub-writing-num">01</span>
        <span class="ub-writing-title">算法基础 — DSA Fundamentals</span>
        <span class="ub-writing-meta">11 篇</span>
        <span class="ub-writing-arrow">↗</span>
      </a>
      <a class="ub-writing-item ub-scroll-reveal" href="/JavaScript 算法/LeetCode Hot100/LeetCode Hot100">
        <span class="ub-writing-num">02</span>
        <span class="ub-writing-title">LeetCode Hot 100 — High-Frequency Interview Problems</span>
        <span class="ub-writing-meta">20 篇</span>
        <span class="ub-writing-arrow">↗</span>
      </a>
      <a class="ub-writing-item ub-scroll-reveal" href="/JavaScript 算法/经典 150/经典 150 数字 字符串">
        <span class="ub-writing-num">03</span>
        <span class="ub-writing-title">经典 150 题 — Classic 150 Problems</span>
        <span class="ub-writing-meta">17 篇</span>
        <span class="ub-writing-arrow">↗</span>
      </a>
      <a class="ub-writing-item ub-scroll-reveal" href="/JavaScript 算法/GRD/GRD 数组">
        <span class="ub-writing-num">04</span>
        <span class="ub-writing-title">GRD 刷题 — Gold Representative Drills</span>
        <span class="ub-writing-meta">11 篇</span>
        <span class="ub-writing-arrow">↗</span>
      </a>
      <a class="ub-writing-item ub-scroll-reveal" href="/JavaScript 算法/解题思路/322 零钱兑换">
        <span class="ub-writing-num">05</span>
        <span class="ub-writing-title">解题思路 — Deep Dive Problem Breakdowns</span>
        <span class="ub-writing-meta">2 篇</span>
        <span class="ub-writing-arrow">↗</span>
      </a>
      <a class="ub-writing-item ub-scroll-reveal" href="/瀑布流项目/项目骨架搭建">
        <span class="ub-writing-num">06</span>
        <span class="ub-writing-title">工程实践 — Frontend Engineering Practice</span>
        <span class="ub-writing-meta">2 篇</span>
        <span class="ub-writing-arrow">↗</span>
      </a>
    </div>
  </div>
</section>

<!-- CONNECT -->
<section class="ub-connect" id="ub-connect">
  <div class="ub-container">
    <div class="ub-section-label ub-scroll-reveal"><span class="ub-label-num">03</span> PARTY UP</div>
    <h2 class="ub-section-title ub-connect-title ub-scroll-reveal">Let's build<br/>something.</h2>
    <div class="ub-social-links">
      <a class="ub-social-link ub-scroll-reveal" href="https://github.com/unbrain" target="_blank">
        <span class="ub-social-platform">GitHub</span>
        <span class="ub-social-handle">@unbrain ↗</span>
      </a>
      <a class="ub-social-link ub-scroll-reveal" href="mailto:marsorsun@gmail.com">
        <span class="ub-social-platform">Email</span>
        <span class="ub-social-handle">marsorsun@gmail.com ↗</span>
      </a>
    </div>
  </div>
</section>

<script>
(function() {
  // ── CUSTOM CURSOR ──
  var cursor = document.getElementById('ub-cursor');
  var follower = document.getElementById('ub-cursorFollower');
  var mx = 0, my = 0, fx = 0, fy = 0;
  document.addEventListener('mousemove', function(e) {
    mx = e.clientX; my = e.clientY;
    cursor.style.left = mx + 'px';
    cursor.style.top  = my + 'px';
  });
  function followLoop() {
    fx += (mx - fx) * 0.12;
    fy += (my - fy) * 0.12;
    follower.style.left = fx + 'px';
    follower.style.top  = fy + 'px';
    requestAnimationFrame(followLoop);
  }
  followLoop();
  document.querySelectorAll('a, button').forEach(function(el) {
    el.addEventListener('mouseenter', function() {
      cursor.style.width = '14px'; cursor.style.height = '14px';
      follower.style.width = '50px'; follower.style.height = '50px';
    });
    el.addEventListener('mouseleave', function() {
      cursor.style.width = '8px'; cursor.style.height = '8px';
      follower.style.width = '36px'; follower.style.height = '36px';
    });
  });

  // ── PARTICLES ──
  var canvas = document.getElementById('ub-particleCanvas');
  if (canvas) {
    var ctx = canvas.getContext('2d');
    var W, H, particles = [], mouse = { x: -999, y: -999 };
    function resize() { W = canvas.width = canvas.offsetWidth; H = canvas.height = canvas.offsetHeight; }
    resize();
    window.addEventListener('resize', resize);
    canvas.addEventListener('mousemove', function(e) {
      var r = canvas.getBoundingClientRect();
      mouse.x = e.clientX - r.left; mouse.y = e.clientY - r.top;
    });
    var COUNT = Math.min(80, Math.floor(window.innerWidth / 16));
    for (var i = 0; i < COUNT; i++) {
      particles.push({ x: Math.random()*W, y: Math.random()*H, vx: (Math.random()-0.5)*0.4, vy: (Math.random()-0.5)*0.4, r: Math.random()*1.5+0.5 });
    }
    function drawParticles() {
      ctx.clearRect(0, 0, W, H);
      particles.forEach(function(p) {
        p.x += p.vx; p.y += p.vy;
        if (p.x < 0) p.x = W; if (p.x > W) p.x = 0;
        if (p.y < 0) p.y = H; if (p.y > H) p.y = 0;
        ctx.beginPath(); ctx.arc(p.x, p.y, p.r, 0, Math.PI*2);
        ctx.fillStyle = 'rgba(254,250,224,0.35)'; ctx.fill();
      });
      for (var i = 0; i < particles.length; i++) {
        for (var j = i+1; j < particles.length; j++) {
          var dx = particles[i].x - particles[j].x, dy = particles[i].y - particles[j].y;
          var dist = Math.sqrt(dx*dx+dy*dy);
          if (dist < 120) {
            ctx.beginPath(); ctx.moveTo(particles[i].x, particles[i].y); ctx.lineTo(particles[j].x, particles[j].y);
            ctx.strokeStyle = 'rgba(254,250,224,'+(0.06*(1-dist/120))+')'; ctx.lineWidth = 0.5; ctx.stroke();
          }
        }
        var dx2 = particles[i].x - mouse.x, dy2 = particles[i].y - mouse.y;
        var dist2 = Math.sqrt(dx2*dx2+dy2*dy2);
        if (dist2 < 160) {
          ctx.beginPath(); ctx.moveTo(particles[i].x, particles[i].y); ctx.lineTo(mouse.x, mouse.y);
          ctx.strokeStyle = 'rgba(254,250,224,'+(0.12*(1-dist2/160))+')'; ctx.lineWidth = 0.6; ctx.stroke();
        }
      }
      requestAnimationFrame(drawParticles);
    }
    drawParticles();
  }

  // ── NAV SCROLL ──
  var nav = document.getElementById('ub-nav');
  window.addEventListener('scroll', function() {
    nav.classList.toggle('scrolled', window.scrollY > 80);
  });

  // ── HERO PARALLAX ──
  var heroContent = document.querySelector('.ub-hero-content');
  window.addEventListener('scroll', function() {
    var scrollY = window.scrollY, vh = window.innerHeight;
    if (heroContent && scrollY < vh) {
      var ratio = scrollY / vh;
      heroContent.style.transform = 'translateY('+scrollY*0.3+'px)';
      heroContent.style.opacity = 1 - ratio * 1.2;
    }
  });

  // ── PROGRESS BAR ──
  var bar = document.getElementById('ub-progressBar');
  window.addEventListener('scroll', function() {
    var scrollTop = window.scrollY;
    var docH = document.documentElement.scrollHeight - window.innerHeight;
    if (bar) bar.style.width = (scrollTop / docH * 100) + '%';
  });

  // ── SCROLL REVEAL ──
  var revealItems = document.querySelectorAll('.ub-scroll-reveal');
  var revealObs = new IntersectionObserver(function(entries) {
    entries.forEach(function(entry) {
      if (entry.isIntersecting) { entry.target.classList.add('visible'); revealObs.unobserve(entry.target); }
    });
  }, { threshold: 0.08 });
  revealItems.forEach(function(item) { revealObs.observe(item); });

  // ── STAT BARS ──
  var bars = document.querySelectorAll('.ub-stat-bar-fill');
  var barObs = new IntersectionObserver(function(entries) {
    entries.forEach(function(entry) {
      if (entry.isIntersecting) {
        var b = entry.target;
        setTimeout(function() { b.style.width = b.dataset.width + '%'; }, 200);
        barObs.unobserve(b);
      }
    });
  }, { threshold: 0.3 });
  bars.forEach(function(b) { barObs.observe(b); });

  // ── SECTION WIPE LINES ──
  document.querySelectorAll('.ub-about, .ub-knowledge, .ub-connect').forEach(function(section) {
    var line = document.createElement('div');
    line.style.cssText = 'position:absolute;top:0;left:0;right:0;height:1px;background:#FEFAE0;transform:scaleX(0);transform-origin:left;transition:transform 1.2s cubic-bezier(0.16,1,0.3,1);z-index:1;';
    section.style.position = 'relative';
    section.prepend(line);
    var obs = new IntersectionObserver(function(entries) {
      if (entries[0].isIntersecting) { line.style.transform = 'scaleX(1)'; obs.disconnect(); }
    }, { threshold: 0 });
    obs.observe(section);
  });

  // ── GLITCH ──
  var heroName = document.querySelector('.ub-reveal-line');
  if (heroName) {
    setInterval(function() {
      heroName.style.textShadow = (Math.random()*4-2)+'px '+(Math.random()*2-1)+'px 0 rgba(254,250,224,0.15), '+(Math.random()*-4+2)+'px '+(Math.random()*2-1)+'px 0 rgba(254,250,224,0.1)';
      setTimeout(function() { heroName.style.textShadow = 'none'; }, 80);
    }, 4000);
  }
  // ── LOAD GLASS CUBE (after particles are running) ──
  setTimeout(function() {
    var s = document.createElement('script');
    s.src = '/static/glass-cube.js';
    document.body.appendChild(s);
  }, 300);

})();
</script>
